name: 'fedramp_20x'
version: '0.0.1'
require-dbt-version: [">=1.3.0", "<2.0.0"]
config-version: 2

model-paths: ["models"]
analysis-paths: []
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]

target-path: "target"
clean-targets:
  - "target"
  - "dbt_packages"

models:
  fedramp_20x:
    fedramp_ksis:
      +materialized: table
    k8s_container_images:
      +materialized: table
    fedramp_ksis:
      +materialized: table

vars:
  default_system_namespaces:
    - "kube-system"
    - "gke-mcs"